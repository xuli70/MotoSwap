/**
 * config.js.template - Template para Variables de Entorno
 * 
 * Este archivo es procesado por envsubst en el Dockerfile
 * Las variables ${VARIABLE_NAME} son reemplazadas por valores reales de Coolify
 * 
 * NUNCA commitear este archivo como config.js
 * Solo commitear como config.js.template
 */

// Configuración principal desde variables de entorno de Coolify
window.APP_CONFIG = {
    // === OBLIGATORIAS ===
    // Tu aplicación NO funcionará sin estas variables
    SUPABASE_URL: '${SUPABASE_URL}',
    SUPABASE_ANON_KEY: '${SUPABASE_ANON_KEY}',
    PASSWORD_USER: '${PASSWORD_USER}',
    PASSWORD_ADMIN: '${PASSWORD_ADMIN}',
    
    // === OPCIONALES ===
    // Solo incluir si tu aplicación las usa
    OPENAI_API_KEY: '${OPENAI_API_KEY}',
    AI_MODEL: '${AI_MODEL}',
    
    // === CONFIGURACIÓN DE APLICACIÓN ===
    APP_NAME: 'MotoSwap',
    ENVIRONMENT: 'production',
    VERSION: '1.0.0',
    
    // === CONFIGURACIÓN DE SEGURIDAD ===
    SECURITY_ENABLED: true,
    SECURITY_STRICT: false,  // No ser demasiado agresivo para UX
    SECURITY_DISABLE_CONTEXT: false,
    SECURITY_DISABLE_DEVKEYS: false,
    
    // === FEATURES ===
    FEATURES: {
        MESSAGING: true,
        REVIEWS: true,
        ADVANCED_SEARCH: true,
        NOTIFICATIONS: false,
        DEBUG_MODE: false  // Deshabilitado en producción
    }
};

// Sistema de compatibilidad dual para código legacy
// Permite usar tanto window.APP_CONFIG como window.env
window.env = {
    SUPABASE_URL: '${SUPABASE_URL}',
    SUPABASE_ANON_KEY: '${SUPABASE_ANON_KEY}',
    PASSWORD_USER: '${PASSWORD_USER}',
    PASSWORD_ADMIN: '${PASSWORD_ADMIN}',
    OPENAI_API_KEY: '${OPENAI_API_KEY}',
    AI_MODEL: '${AI_MODEL}',
    ENVIRONMENT: 'production'
};

// Función de validación de configuración
window.validateConfig = function() {
    const config = window.APP_CONFIG;
    const required = ['SUPABASE_URL', 'SUPABASE_ANON_KEY', 'PASSWORD_USER', 'PASSWORD_ADMIN'];
    const missing = required.filter(key => !config[key] || config[key] === '${' + key + '}');
    
    if (missing.length > 0) {
        console.error('❌ Variables de entorno faltantes:', missing);
        console.error('💡 Verificar configuración en Coolify');
        return false;
    }
    
    // Validar formato de URLs
    if (config.SUPABASE_URL && !config.SUPABASE_URL.startsWith('https://')) {
        console.error('❌ SUPABASE_URL debe comenzar con https://');
        return false;
    }
    
    // Validar formato de clave anónima de Supabase
    if (config.SUPABASE_ANON_KEY && !config.SUPABASE_ANON_KEY.startsWith('eyJ')) {
        console.error('❌ SUPABASE_ANON_KEY no parece válida (debe comenzar con eyJ)');
        return false;
    }
    
    console.log('✅ Configuración validada correctamente');
    console.log('🌐 Entorno:', config.ENVIRONMENT);
    console.log('🔗 Supabase URL:', config.SUPABASE_URL);
    console.log('🛡️ Seguridad:', config.SECURITY_ENABLED ? 'ACTIVADA' : 'DESACTIVADA');
    
    return true;
};

// Función de inicialización segura
window.initializeApp = function() {
    console.log('🚀 Inicializando MotoSwap en modo PRODUCCIÓN...');
    
    // Validar configuración
    if (!window.validateConfig()) {
        console.error('❌ Error crítico en configuración. La aplicación no se puede inicializar.');
        return false;
    }
    
    // Inicializar seguridad si está disponible
    if (window.MotoSwapSecurity && window.APP_CONFIG.SECURITY_ENABLED) {
        window.MotoSwapSecurity.configure({
            strict: window.APP_CONFIG.SECURITY_STRICT,
            disableContext: window.APP_CONFIG.SECURITY_DISABLE_CONTEXT,
            disableDevKeys: window.APP_CONFIG.SECURITY_DISABLE_DEVKEYS
        });
        console.log('🛡️ Sistema de seguridad configurado');
    }
    
    // Configurar cliente Supabase si está disponible
    if (window.supabase && window.APP_CONFIG.SUPABASE_URL && window.APP_CONFIG.SUPABASE_ANON_KEY) {
        try {
            window.supabaseClient = window.supabase.createClient(
                window.APP_CONFIG.SUPABASE_URL,
                window.APP_CONFIG.SUPABASE_ANON_KEY
            );
            console.log('🗄️ Cliente Supabase inicializado');
        } catch (error) {
            console.error('❌ Error inicializando Supabase:', error.message);
            return false;
        }
    }
    
    console.log('✅ MotoSwap inicializado correctamente');
    return true;
};

// Log de confirmación
console.log('🚀 config.js generado desde variables de entorno (Coolify)');
console.log('🔍 Validando configuración...');

// Auto-validación e inicialización
if (window.validateConfig()) {
    console.log('⏳ Aplicación lista para inicializar');
} else {
    console.error('💥 Error crítico en configuración');
}

/**
 * INSTRUCCIONES DE USO:
 * 
 * 1. DESARROLLO LOCAL:
 *    - Copiar config.js.example como config.js
 *    - NO usar este template para desarrollo local
 * 
 * 2. PRODUCCIÓN (COOLIFY):
 *    - Este archivo se procesa automáticamente
 *    - Variables ${VAR} se reemplazan por valores reales
 *    - Resultado final es config.js funcional
 * 
 * 3. VARIABLES REQUERIDAS EN COOLIFY:
 *    - SUPABASE_URL: URL de tu proyecto Supabase
 *    - SUPABASE_ANON_KEY: Clave anónima de Supabase  
 *    - PASSWORD_USER: Contraseña para usuarios regulares
 *    - PASSWORD_ADMIN: Contraseña para administradores
 * 
 * 4. VARIABLES OPCIONALES:
 *    - OPENAI_API_KEY: Para funciones de IA
 *    - AI_MODEL: Modelo de IA (default: gpt-4o-mini)
 * 
 * 5. PERSONALIZACIÓN:
 *    - Agregar nuevas variables en sección correspondiente
 *    - Configurar las variables en Coolify
 *    - Reconstruir y deployar
 * 
 * 6. DEBUGGING:
 *    - Verificar logs: "config.js generado exitosamente"
 *    - Si falla: verificar variables en Coolify
 *    - Si undefined: verificar sintaxis ${VARIABLE}
 * 
 * 7. INICIALIZACIÓN:
 *    - Llamar window.initializeApp() en index.html
 *    - Verificar que todas las validaciones pasen
 *    - Comprobar que Supabase se conecta correctamente
 */