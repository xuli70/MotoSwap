/**
 * config.js.example - Configuración para Desarrollo Local
 * 
 * INSTRUCCIONES:
 * 1. Copiar este archivo como 'config.js' en el mismo directorio
 * 2. Modificar los valores con tus credenciales reales
 * 3. NUNCA commitear config.js al repositorio (está en .gitignore)
 * 4. Solo usar para desarrollo local, NO para producción
 */

// Configuración para desarrollo local
window.APP_CONFIG = {
    // === OBLIGATORIAS - Supabase ===
    // Obtener de tu proyecto Supabase: Settings → API
    SUPABASE_URL: 'https://tuproyecto.supabase.co',  // Cambiar por tu URL real
    SUPABASE_ANON_KEY: 'eyJ...tu_clave_anonima_aqui',  // Cambiar por tu clave real
    
    // === OBLIGATORIAS - Autenticación ===
    PASSWORD_USER: 'usuario123',      // Contraseña para usuarios regulares
    PASSWORD_ADMIN: 'admin123',       // Contraseña para administradores
    
    // === OPCIONALES - IA ===
    OPENAI_API_KEY: '',               // Opcional: para funciones de IA
    AI_MODEL: 'gpt-4o-mini',         // Modelo de IA por defecto
    
    // === CONFIGURACIÓN DE APLICACIÓN ===
    APP_NAME: 'MotoSwap',
    ENVIRONMENT: 'development',
    VERSION: '1.0.0',
    
    // === CONFIGURACIÓN DE SEGURIDAD ===
    SECURITY_ENABLED: true,
    SECURITY_STRICT: false,          // No ser agresivo en desarrollo
    SECURITY_DISABLE_CONTEXT: false, // Permitir click derecho en desarrollo
    SECURITY_DISABLE_DEVKEYS: false, // Permitir F12 en desarrollo
    
    // === FEATURES ===
    FEATURES: {
        MESSAGING: true,              // Sistema de mensajería
        REVIEWS: true,                // Sistema de valoraciones
        ADVANCED_SEARCH: true,        // Búsqueda avanzada
        NOTIFICATIONS: false,         // Notificaciones (futuro)
        DEBUG_MODE: true              // Logs extra para desarrollo
    }
};

// Compatibilidad dual para código legacy
window.env = {
    SUPABASE_URL: window.APP_CONFIG.SUPABASE_URL,
    SUPABASE_ANON_KEY: window.APP_CONFIG.SUPABASE_ANON_KEY,
    PASSWORD_USER: window.APP_CONFIG.PASSWORD_USER,
    PASSWORD_ADMIN: window.APP_CONFIG.PASSWORD_ADMIN,
    OPENAI_API_KEY: window.APP_CONFIG.OPENAI_API_KEY,
    AI_MODEL: window.APP_CONFIG.AI_MODEL,
    ENVIRONMENT: 'development'
};

// Función de validación para desarrollo
window.validateConfig = function() {
    const config = window.APP_CONFIG;
    
    console.log('🏠 DESARROLLO LOCAL - Validando configuración...');
    
    // Verificar Supabase
    if (!config.SUPABASE_URL || config.SUPABASE_URL.includes('tuproyecto')) {
        console.warn('⚠️ SUPABASE_URL no configurada. Usando datos mock.');
        console.warn('💡 Para usar datos reales: configurar SUPABASE_URL en config.js');
        return false;
    }
    
    if (!config.SUPABASE_ANON_KEY || config.SUPABASE_ANON_KEY.includes('tu_clave')) {
        console.warn('⚠️ SUPABASE_ANON_KEY no configurada. Usando datos mock.');
        console.warn('💡 Para usar datos reales: configurar SUPABASE_ANON_KEY en config.js');
        return false;
    }
    
    // Validar formato
    if (!config.SUPABASE_URL.startsWith('https://')) {
        console.error('❌ SUPABASE_URL debe comenzar con https://');
        return false;
    }
    
    if (!config.SUPABASE_ANON_KEY.startsWith('eyJ')) {
        console.error('❌ SUPABASE_ANON_KEY debe comenzar con eyJ');
        return false;
    }
    
    console.log('✅ Configuración de Supabase válida');
    console.log('🗄️ Conectando con base de datos real...');
    
    return true;
};

// Función de inicialización para desarrollo
window.initializeApp = function() {
    console.log('🏠 Inicializando MotoSwap en modo DESARROLLO...');
    console.log('🐛 Debug mode:', window.APP_CONFIG.FEATURES.DEBUG_MODE ? 'ON' : 'OFF');
    
    // En desarrollo, validación más permisiva
    const hasSupabase = window.validateConfig();
    
    if (!hasSupabase) {
        console.log('📝 Usando datos mock para desarrollo');
        console.log('💡 TIP: Configurar Supabase para usar datos reales');
    }
    
    // Inicializar seguridad (modo menos estricto)
    if (window.MotoSwapSecurity) {
        window.MotoSwapSecurity.configure({
            strict: false,      // No ser estricto en desarrollo
            disableContext: false,  // Permitir click derecho
            disableDevKeys: false   // Permitir F12
        });
        console.log('🛡️ Seguridad configurada para desarrollo');
    }
    
    // Inicializar Supabase si está configurado
    if (hasSupabase && window.supabase) {
        try {
            window.supabaseClient = window.supabase.createClient(
                window.APP_CONFIG.SUPABASE_URL,
                window.APP_CONFIG.SUPABASE_ANON_KEY
            );
            console.log('🗄️ Cliente Supabase inicializado');
        } catch (error) {
            console.error('❌ Error inicializando Supabase:', error.message);
            console.log('📝 Continuando con datos mock...');
        }
    }
    
    console.log('✅ MotoSwap (desarrollo) inicializado');
    return true;
};

// Herramientas de desarrollo
window.MotoSwapDev = {
    // Mostrar configuración actual
    showConfig: function() {
        console.table(window.APP_CONFIG);
    },
    
    // Toggle debug mode
    toggleDebug: function() {
        window.APP_CONFIG.FEATURES.DEBUG_MODE = !window.APP_CONFIG.FEATURES.DEBUG_MODE;
        console.log('🐛 Debug mode:', window.APP_CONFIG.FEATURES.DEBUG_MODE ? 'ON' : 'OFF');
    },
    
    // Toggle feature
    toggleFeature: function(feature) {
        if (window.APP_CONFIG.FEATURES.hasOwnProperty(feature)) {
            window.APP_CONFIG.FEATURES[feature] = !window.APP_CONFIG.FEATURES[feature];
            console.log(`🎛️ Feature ${feature}:`, window.APP_CONFIG.FEATURES[feature] ? 'ON' : 'OFF');
        } else {
            console.log('❌ Feature not found:', feature);
        }
    },
    
    // Probar conexión Supabase
    testSupabase: async function() {
        if (!window.supabaseClient) {
            console.log('❌ Supabase no configurado');
            return;
        }
        
        try {
            const { data, error } = await window.supabaseClient
                .from('users')
                .select('count')
                .limit(1);
            
            if (error) throw error;
            console.log('✅ Conexión Supabase OK');
        } catch (error) {
            console.error('❌ Error probando Supabase:', error.message);
        }
    }
};

// Log de inicialización
console.log('🏠 config.js cargado para DESARROLLO LOCAL');
console.log('🔍 Validando configuración...');

// Auto-validación
window.validateConfig();

// Mostrar herramientas disponibles
console.log('🛠️ Herramientas de desarrollo disponibles:');
console.log('   MotoSwapDev.showConfig() - Mostrar configuración');
console.log('   MotoSwapDev.toggleDebug() - Toggle debug mode');
console.log('   MotoSwapDev.toggleFeature("FEATURE_NAME") - Toggle feature');
console.log('   MotoSwapDev.testSupabase() - Probar conexión BD');

/**
 * PASOS PARA CONFIGURAR:
 * 
 * 1. OBTENER CREDENCIALES SUPABASE:
 *    - Ir a https://app.supabase.com
 *    - Crear/seleccionar proyecto
 *    - Settings → API
 *    - Copiar URL y anon key
 * 
 * 2. CONFIGURAR config.js:
 *    - Copiar este archivo como config.js
 *    - Reemplazar SUPABASE_URL con tu URL real
 *    - Reemplazar SUPABASE_ANON_KEY con tu clave real
 *    - Ajustar contraseñas si necesario
 * 
 * 3. TESTING:
 *    - Abrir index.html en navegador
 *    - Verificar console: "Cliente Supabase inicializado"
 *    - Probar: MotoSwapDev.testSupabase()
 * 
 * 4. DEBUGGING:
 *    - Usar MotoSwapDev.showConfig() para ver configuración
 *    - Activar debug: MotoSwapDev.toggleDebug()
 *    - Ver logs extra en console
 * 
 * 5. FEATURES:
 *    - Activar/desactivar: MotoSwapDev.toggleFeature("MESSAGING")
 *    - Probar diferentes funcionalidades
 *    - Verificar comportamiento en desarrollo
 */